name: Run tests on kedro-docker

on:
  push:
    paths:
      - "kedro-docker/**"
  pull_request:
    types: [ opened, synchronize ]

jobs:
  datasets-test:
    uses: ./.github/workflows/unit-test.yml
    with:
      plugin: kedro-docker
