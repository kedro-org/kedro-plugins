name: Run checks on Kedro-Airflow

on:
  push:
    paths-ignore:
       - "kedro-datasets/**"
       - "kedro-docker/**"
       - "kedro-telemetry/**"
  pull_request:
    paths-ignore:
       - "kedro-datasets/**"
       - "kedro-docker/**"
       - "kedro-telemetry/**"
    types: [ synchronize ]

jobs:
  airflow-test:
    uses: ./.github/workflows/check-plugin.yml
    with:
      plugin: kedro-airflow
